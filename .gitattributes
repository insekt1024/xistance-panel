# Shell + node scripts must keep LF endings on Windows checkouts.
*.sh text eol=lf
*.mjs text eol=lf
# Runtime tunnel data is gitignored, but never let git touch binaries if added.
tunnels/bin/* -text
