{
  "name": "xistance-panel",
  "version": "1.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w @xistance/types && npm run build -w @xistance/tunnel-core && npm run build -w @xistance/i18n && npm run build -w @xistance/db",
    "dev": "npm run build:packages && npm run dev -w @xistance/web",
    "build": "npm run build:packages && npm run build -w @xistance/web",
    "start": "npm run start -w @xistance/web",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit -p apps/web && tsc --noEmit -p packages/types && tsc --noEmit -p packages/tunnel-core && tsc --noEmit -p packages/db",
    "version:show": "node scripts/version.mjs --show",
    "version:check": "node scripts/version.mjs --check",
    "version:bump": "node scripts/version.mjs"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
